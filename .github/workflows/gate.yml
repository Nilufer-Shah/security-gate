on:
  workflow_call:
    inputs:
      python-path:
        type: string
        default: ''
      node-path:
        type: string
        default: ''
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "python=${{ inputs.python-path }} node=${{ inputs.node-path }}"
